currentBuild.displayName = " Surya - "+currentBuild.number

pipeline {
    agent any
    stages{
        stage('hello world'){
           steps{
             echo "hello world ${BUILD_DISPLAY_NAME}"
             echo "${JOB_NAME}"
           }
        }
    }
}